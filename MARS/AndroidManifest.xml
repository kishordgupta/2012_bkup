<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mars"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="Home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
       	<activity android:name=".MainActivity" android:label="Menu"/>
       	<activity android:name=".CategoryActivity" android:label="Category"/>
       	<activity android:name=".SubCategoryActivity" android:label="Sub Category"/>
       	<activity android:name=".ProductActivity" android:label="Product List"/>
       	<activity android:name=".ProductDetailActivity" android:label="Product Detail"/>
       	<activity android:name=".ForgotPassActivity" android:label="Forgot Password"/>
       	<activity android:name=".InboxActivity" android:label="Inbox"/>
       	<activity android:name=".LoginActivity" android:label="Login"/>
       	<activity android:name=".ProfileActivity" android:label="Profile"/>
       	<activity android:name=".WishListActivity" android:label="Wish List"/>
       	<activity android:name=".SignupActivity" android:label="Signup"/>
    </application>

</manifest>